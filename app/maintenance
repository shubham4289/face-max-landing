// app/maintenance/page.tsx
export const metadata = {
  title: "Be right back — Face Max Academy",
  robots: { index: false, follow: false }, // avoid indexing the maintenance page
};

export default function MaintenancePage() {
  return (
    <div className="min-h-screen bg-gradient-to-b from-white to-slate-50 text-slate-900 flex items-center justify-center px-6">
      <div className="max-w-xl text-center">
        <img src="/logo.png" alt="Face Max Academy" className="h-10 w-auto mx-auto mb-4" />
        <h1 className="text-3xl md:text-4xl font-extrabold tracking-tight">
          We’re polishing your course trailer ✨
        </h1>
        <p className="mt-3 text-slate-600">
          The site is temporarily unavailable while we finalize the trailer. Enrollment reopens shortly.
        </p>

        <div className="mt-6 inline-flex items-center gap-2 rounded-2xl px-5 py-3 bg-slate-900 text-white font-semibold">
          Back online soon
        </div>

        <p className="mt-4 text-sm text-slate-500">
          Questions? Email <a className="underline" href="mailto:admin@thefacemax.com">admin@thefacemax.com</a>
        </p>

        <div className="mt-8 text-xs text-slate-400">
          Lifetime access • Certificate • Continuous updates
        </div>
      </div>
    </div>
  );
}
